{
  "name": "start-ts",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --cls --transpile-only ./src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && node ./dist/",
    "build-webpack": "webpack"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "ts-loader": "^9.5.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
